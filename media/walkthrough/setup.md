# Setup Whisper

Run **Voice Agent: Setup Whisper** once. This creates a persistent venv under `~/.local/share/voice-agent/.venv` and downloads the Whisper model.

On Debian/Ubuntu you need `python3-venv`.
