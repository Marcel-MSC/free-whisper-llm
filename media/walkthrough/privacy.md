# Privacy

- Audio never leaves your machine (local Whisper).
- The transcript and editor context (active file / selection / paths; Pro also sends search snippets) go to **your** LLM endpoint.
- Local Ollama ≈ stays local. Cloud providers = leaves the machine.
