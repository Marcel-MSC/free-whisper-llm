# Configure your LLM

- **Ollama (recommended):** install Ollama, `ollama pull llama3.2`, keep provider = `ollama`.
- **OpenAI / Anthropic:** set provider + model, then run **Voice Agent: Set API Key**.

Keys are stored in Secret Storage, not settings sync.
